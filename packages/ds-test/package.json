{
  "name": "ds-test",
  "version": "1.0.1",
  "private": true,
  "description": "Unofficial NPM distribution of DS Test",
  "license": "GPL-3.0-or-later",
  "author": "Foundry",
  "scripts": {
    "build": "cp -r ../../lib/ds-test/src/* .",
    "fix": "cd ../.. && yarn run fix",
    "postinstall": "yarn run build"
  }
}
